<section id='suppliers'>
  <h2>Fornecedores</h2>

  <% if @suppliers.empty? %>
    <p>Não existem fornecedores cadastrados.</p>
  <% else %>
    <% @suppliers.each do |supplier| %>
      <div>
        <h3><%= link_to supplier.brand_name, supplier %></h3>
      </div>
    <% end %>
  <% end %>
</section>

<div>
  <p><%= link_to 'Voltar', root_path %></p>
</div>
